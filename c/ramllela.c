#include <stdio.h>
int main()
{
	int c,d,n1,n2,i,j,s;
	scanf("%d%d%d%d",&d,&c,&n1,&n2);
	char a=[c+d];
	if(c>=1 && c<=1000 && d>=1 && d<=1000 &&n1>=1 && n1<=20 && n2>=1 &&n2<=20)
	{
		for(i=0;i<c+d;i++)
		{
			if
		}
		
	}
	
	else
	printf("wrong input");
}
